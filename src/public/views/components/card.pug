mixin card(icon, title, description, url, external, color)
    a#link(href=url, target=`${external === 'on' ? '_blank':''}`)
        .card(style=`background:${color}`)
            .content
                .icon
                    i(class=icon).fa
                .title=title
                p= description 